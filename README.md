Para incluir una nueva vista en SRC añade una carpeta con el nombre de la vista, 
dentro haz un archivo html, css y js (IMPORTANTE TODO CON EL MISMO NOMBRE SIENDO CASE SENSITIVE).
Para pasar a otra página se tiene que utilizar la función renderize del objeto frameworkUtils 
con el nombre de la vista